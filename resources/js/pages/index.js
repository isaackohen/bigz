import bitcoin from 'bitcoin-units';

let destroy = null, init;
$.on('/', function(){

$('.game_poster').tilt({
    glare: false,
    scale: 1.02
})



$('.loff').owlCarousel({
    loop:true,
    autoplay:true,
    margin:5,
    autoplaySpeed: 500,
    items:1,
    nav:false,
    dots: false,
    autoplayTimeout:7000

})



}, ['/css/pages/index.css']);
